<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devculture.apiconsumer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.SelectApiActivity" />
        <activity
            android:name=".controllers.reddit.RedditTopAuthActivity"
            android:label="Reddit Top Posts">
        </activity>
        <activity
            android:name=".controllers.rottentomatoes.RottenTomatoesTopMoviesActivity"
            android:label="Rotten Tomatoes Top Movies" />
        <activity
            android:name=".controllers.reddit.RedditTopListActivity"
            android:label="@string/title_reddit_list"
            android:parentActivityName=".controllers.BaseActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.devculture.apiconsumer.controllers.BaseActivity" />
        </activity>
        <activity
            android:name=".controllers.reddit.RedditTopDetailActivity"
            android:label="@string/title_reddit_detail"
            android:parentActivityName=".controllers.reddit.RedditTopListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.devculture.apiconsumer.controllers.reddit.RedditTopListActivity" />
        </activity>
    </application>

</manifest>